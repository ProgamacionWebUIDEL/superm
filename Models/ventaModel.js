class Venta {
    constructor(id, nombre, direccion,producto,cantidad ,precioTotal) {
      (this.id = id),
        (this.nombre = nombre),
        (this.direccion = direccion),
        (this.producto = producto),
        (this.cantidad = cantidad),
        (this.precioTotal = precioTotal);
    }
  }
  
  export default Venta;