class Cliente {
    constructor(id, name, apellido, mail, telefono, direccion, retailer, amountInStock) {
      (this.id = id),
        (this.name = name),
        (this.price = apellido),
        (this.price = mail),
        (this.price = telefono),
        (this.price = direccion),
        (this.retailer = retailer),
        (this.amountInStock = amountInStock);
    }
  }
  
  export default Cliente;